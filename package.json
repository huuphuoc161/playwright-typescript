{
  "devDependencies": {
    "@playwright/test": "^1.53.2",
    "@types/node": "^24.0.7",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "pretest": "tsc --incremental -p playwright-mobile/tsconfig.json",
    "test": "playwright test -c tests-out"
  }
}
